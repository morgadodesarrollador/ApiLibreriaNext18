import useSWR, { SWRConfiguration } from 'swr';
import { ICliente } from '../database/interfaces/clientes/IClientes';


export const useCliebtes = (url: string, config: SWRConfiguration={}) => {
    
}